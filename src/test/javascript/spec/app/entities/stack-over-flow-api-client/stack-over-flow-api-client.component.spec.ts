/* tslint:disable max-line-length */
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Observable, of } from 'rxjs';
import { HttpHeaders, HttpResponse } from '@angular/common/http';
import { ActivatedRoute, Data } from '@angular/router';

import { EtlBigdataTestModule } from '../../../test.module';
import { StackOverFlowApiClientComponent } from 'app/entities/stack-over-flow-api-client/stack-over-flow-api-client.component';
import { StackOverFlowApiClientService } from 'app/entities/stack-over-flow-api-client/stack-over-flow-api-client.service';
import { StackOverFlowApiClient } from 'app/shared/model/stack-over-flow-api-client.model';

describe('Component Tests', () => {
    describe('StackOverFlowApiClient Management Component', () => {
        let comp: StackOverFlowApiClientComponent;
        let fixture: ComponentFixture<StackOverFlowApiClientComponent>;
        let service: StackOverFlowApiClientService;

        beforeEach(() => {
            TestBed.configureTestingModule({
                imports: [EtlBigdataTestModule],
                declarations: [StackOverFlowApiClientComponent],
                providers: [
                    {
                        provide: ActivatedRoute,
                        useValue: {
                            data: {
                                subscribe: (fn: (value: Data) => void) =>
                                    fn({
                                        pagingParams: {
                                            predicate: 'id',
                                            reverse: false,
                                            page: 0
                                        }
                                    })
                            }
                        }
                    }
                ]
            })
                .overrideTemplate(StackOverFlowApiClientComponent, '')
                .compileComponents();

            fixture = TestBed.createComponent(StackOverFlowApiClientComponent);
            comp = fixture.componentInstance;
            service = fixture.debugElement.injector.get(StackOverFlowApiClientService);
        });

        it('Should call load all on init', () => {
            // GIVEN
            const headers = new HttpHeaders().append('link', 'link;link');
            spyOn(service, 'query').and.returnValue(
                of(
                    new HttpResponse({
                        body: [new StackOverFlowApiClient(123)],
                        headers
                    })
                )
            );

            // WHEN
            comp.ngOnInit();

            // THEN
            expect(service.query).toHaveBeenCalled();
            expect(comp.stackOverFlowApiClients[0]).toEqual(jasmine.objectContaining({ id: 123 }));
        });

        it('should load a page', () => {
            // GIVEN
            const headers = new HttpHeaders().append('link', 'link;link');
            spyOn(service, 'query').and.returnValue(
                of(
                    new HttpResponse({
                        body: [new StackOverFlowApiClient(123)],
                        headers
                    })
                )
            );

            // WHEN
            comp.loadPage(1);

            // THEN
            expect(service.query).toHaveBeenCalled();
            expect(comp.stackOverFlowApiClients[0]).toEqual(jasmine.objectContaining({ id: 123 }));
        });

        it('should re-initialize the page', () => {
            // GIVEN
            const headers = new HttpHeaders().append('link', 'link;link');
            spyOn(service, 'query').and.returnValue(
                of(
                    new HttpResponse({
                        body: [new StackOverFlowApiClient(123)],
                        headers
                    })
                )
            );

            // WHEN
            comp.loadPage(1);
            comp.reset();

            // THEN
            expect(comp.page).toEqual(0);
            expect(service.query).toHaveBeenCalledTimes(2);
            expect(comp.stackOverFlowApiClients[0]).toEqual(jasmine.objectContaining({ id: 123 }));
        });
        it('should calculate the sort attribute for an id', () => {
            // WHEN
            const result = comp.sort();

            // THEN
            expect(result).toEqual(['id,asc']);
        });

        it('should calculate the sort attribute for a non-id attribute', () => {
            // GIVEN
            comp.predicate = 'name';

            // WHEN
            const result = comp.sort();

            // THEN
            expect(result).toEqual(['name,asc', 'id']);
        });
    });
});
